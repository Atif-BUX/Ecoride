# Copy this file to `.env.local` and adjust the values to match your local database server.
# `.env.local` is ignored by Git so your secrets stay private.

DB_HOST=localhost
DB_NAME=ecoride_db
DB_USER=root
DB_PASS=root
DB_PORT=8889
DB_SOCKET=
